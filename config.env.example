# Copy to config.env. Never commit config.env.
# No real secrets exist yet — Cloudflare Access gates the hostname, not the
# app. Kept for parity with the other projects' env_file convention.

PORT=4100
DB_PATH=/app/data/notebox.db
UPLOAD_DIR=/app/data/uploads
MAX_UPLOAD_MB=100
